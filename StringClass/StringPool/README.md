**This project demonstrates how Java Strings behave in memory. 
It shows the difference between string literals in the pool, strings created with new in the heap, and the use of the intern() method.**
