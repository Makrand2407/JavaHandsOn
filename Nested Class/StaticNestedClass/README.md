**This project demonstrates the use of static nested classes in Java.
It shows how a static nested class can access static members of its outer class directly, and how to instantiate a static nested class object without needing an outer class object.**
