**This project demonstrates object cloning in Java using the Cloneable interface.
It shows how the clone() method creates a shallow copy, meaning that object references are shared between the original and cloned objects.**
